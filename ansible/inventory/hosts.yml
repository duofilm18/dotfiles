all:
  children:
    wsl:
      hosts:
        localhost:
          ansible_connection: local

    rpi:
      hosts:
        rpi5b:
          ansible_host: 192.168.88.10
          ansible_user: duofilm
        # tinkerboard:
        #   ansible_host: 192.168.88.XX
        #   ansible_user: root
