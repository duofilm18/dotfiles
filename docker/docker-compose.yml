services:
  guardrails:
    build:
      context: ./guardrails
      dockerfile: Dockerfile
    container_name: guardrails
    profiles:
      - ai
      - guardrails
    volumes:
      - ../shared:/app/shared:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
